#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <unordered_map>

using namespace std;

int main() {
    string fileName = "Strings.txt";
    ifstream inputStream(fileName);
    vector<string> strings;

    string line1;
    string num = "";
    int total = 0;
    int startIndex;
    bool counted = false;
}